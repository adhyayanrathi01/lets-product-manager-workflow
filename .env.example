# Store real values in .env or a secret manager. Never commit them.
# Add variables for the QA personas defined in context.md.
# Suggested convention: TEST_<ROLE>_USERNAME and TEST_<ROLE>_PASSWORD.
TEST_EXAMPLE_USERNAME=
TEST_EXAMPLE_PASSWORD=
